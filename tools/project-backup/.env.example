# Скопируйте в .env и подставьте значения (файл .env в git не коммитить).

# Postgres project DB (только чтение)
DATABASE_URL=postgresql+psycopg2://USER:PASS@HOST:6432/dc-project-korovas?sslmode=require

# S3 / Yandex Object Storage
S3_ENDPOINT_URL=https://storage.yandexcloud.net
S3_BUCKET=dc-project-korovas
S3_PREFIX=

# AWS CLI profile (если настроен aws configure --profile yandex)
AWS_PROFILE=yandex

# Git-репозиторий проекта (collector/config.json, media/, viz.json)
GIT_REMOTE=git@github.com:org/my-project.git
GIT_REF=main

# Папка бэкапа (локально, только запись сюда)
OUTPUT_DIR=%USERPROFILE%\Desktop\backup\korovas

# --- Restore в локальный test_dev (см. restore_project.py) ---
# TARGET_DATABASE_URL=postgresql://collector:collector@localhost:55432/proj_korovas
# TARGET_S3_ENDPOINT_URL=http://localhost:9000
# TARGET_S3_BUCKET=dc-packages
# TARGET_S3_KEY=minioadmin
# TARGET_S3_SECRET=minioadmin
# TARGET_S3_PREFIX=
