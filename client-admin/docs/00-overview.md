# Обзор

Краткая схема — в [корневом README](../README.md). Здесь — детали по экранам и виджетам.

**client-admin** — SPA для staff: правка `manifest.data`, медиа, (демо) визуализация.

**django_server** — приём пакетов с мобилки, хранение, API `/admin-api/v1/*`.

**datapipe_test** — заглушка GT/inference/depth до появления API на платформе.
